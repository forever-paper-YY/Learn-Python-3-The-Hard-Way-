时间：2019年11月19日

地点：公司

正文：

1、代码

print("Let's practice everything.")
print('You\'d need to know \'bout escapes with \\ that do:')             # 转义序列\
print('\n newlines and \t tabs.')                                        # 转义序列\n和\t

poem = """                                                               # 三引号用于多行转行
\tThe lovely world
with logic so firmly planted
cannot discern \n the needs of love
nor comprehend passion from intuition
and requires an explanation
\n\t\twhere there is none.
"""

print("----------")
print(poem)
print("----------")

five = 10 - 2 + 3 - 6
print(f"This should be five: {five}")

def secret_formula(started):                                             # 定义函数
    jelly_beans = started * 500
    jars = jelly_beans / 1000
    crates = jars / 100
    return jelly_beans, jars, crates                                     # return函数用于返回结果

start_point = 10000
beans, jars, crates = secret_formula(start_point)                        # 运行函数

# remember that this is another way to format a string
print("With a starting point of: {}.format(start_point)")                # 第二种格式化
# it's just like with an f"" string
print(f"We'd have {beans} beans, {jars} jars, and {crates} crates.")     # 第一种格式化

start_point = start_point / 10

print("We can also do that this way:")
formula = secret_formula(start_point)                                    # 运行函数
# this is an easy way to apply a list to a format string
print("We'd have {} beans, {} jars, and {} crates.".formate(*formula))

2、笔记
复习之前的知识点




